declare {
  def acc(l, n) {
    match l {
      case (l1::nil) && (true || (false && true)) => {n+l1}
      case (l1::l2) => {acc(l2, n+l1)}
      else {0}
    }
  }
  def sumList(l) {
    acc(l, 0)
  }
  var list = (1::2::3::nil)
  browse(sumList(list))
}